<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wt.myspringcloud.demo.mapper.DemoMapper">

    <select id="testForeach" resultType="com.wt.myspringcloud.common.pojo.entity.WtUser">
        select * from wt_user
        where name in <foreach collection="nameList" item="name" open="(" separator="," close=")">#{name}</foreach>
    </select>

</mapper>